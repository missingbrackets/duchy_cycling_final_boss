# Copy this to .streamlit/secrets.toml and fill in your values.
# APP_PASSWORD is optional — if set, a password gate is shown.
# APP_PASSWORD = "changeme"

# DATABASE_URL is optional — defaults to local SQLite.
# DATABASE_URL = "sqlite:///./duchy_coaching.db"
